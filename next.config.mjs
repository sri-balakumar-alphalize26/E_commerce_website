/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* Odoo backend (see odoo-headless-storefront): images stream through /api/image/<id> */
};
export default nextConfig;
